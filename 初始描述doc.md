总体描述：这是一个GEO引擎平台，
核心流程：
1. 行业知识资产入库：建立企业专属知识库
2. 结构化治理：
3. 意图 / 关键词挖掘：根据用户的行业，创建和管理用户意图列表，并且通过AI挖掘关键词挖掘
4. 生成式内容优化（写作 / 改写）：选择关键产品或内容的相关意图关键词，用AI生成不同风格话的内容用于发布，相当于智能内容生成引擎：按 “意图 + 关键词 + 知识切片” 批量生成符合AI搜索匹配范式的产品。
5. 发布：对接各个平台，可以投流发布相对应的文章
6.监测：建立自动化检测平台搜索情况的比对和衡量指标
7.产品保持迭代


核心功能：
知识资产管理：导入、清洗、图谱、版本
内容结构化引擎：切片、重排、Schema、证据强化
意图挖掘系统：词库、意图分层、问题裂变
GEO 智能写作：模板、多模型适配、幻觉抑制、批量生成
发布与链接管理：多渠道、锚文本、引用矩阵
GEO 监测与分析：引用率、准确率、流量、竞品看板
A/B 测试与迭代：版本对比、效果分析、一键重写





整体结构设计：

用户浏览器 → 前端页面（Vue/React）→ CDN
        ↓
后端API服务（接口+鉴权+计费）
    技术采用python FastAPI 工具框架，遵守 RESTful 设计规范 / 风格
        ↓
三大核心模块：
  1. 用户系统（注册/登录/订单/套餐）
  2. 计费系统（按量/套餐/扣费）
  3. AI调度层（调用通义千问/DeepSeek v4/seedream/image2等）
        ↓
数据库（用户/订单/用量日志）
    mysql+redis
        ↓
云服务器 + 对象存储 + 域名 + SSL
    云数据库 MySQL + 对象存储 OSS + 短信服务 +域名备案




前端：
帮我写一个AI服务平台的前端页面，使用Vue3 + Element Plus + JS，包含：
1. 响应式首页（导航、AI服务介绍、价格套餐）
2. 用户登录注册页面
3. AI对话聊天页面（仿ChatGPT）
4. 个人中心（我的订单、我的用量、充值）
5. 调用后端API接口
要求代码可直接运行，结构清晰

后端：
帮我写一个AI付费服务平台的后端接口，使用Python FastAPI：
1. 用户注册、登录、JWT鉴权
2. 套餐购买、订单创建、支付回调接口
3. 点数计费：用户调用AI接口时扣除点数
4. 调用DeepSeek API接口（先采用这一个方案，后续扩展支持其他方向的接口）
5. 返回标准JSON格式


工期计划：
1、先做最小可用版本：登录 → 付费 → 调用 AI → 扣费
